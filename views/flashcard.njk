<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8"/>
		<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
		<title>{{ name }}</title>
		{# interesting.. is the styling broken? i wonder why.. #}
	</head>
	<body>
		<h1 class="title" id="title">{{ name }}</h1>
		<div class="question-cards" id="questions">

			{% for question in questions %}
				<div class="question-card">
					<p>{{question.q}}</p>
					<p>{{question.a}}</p>
				</div>
			{% endfor %}
		</div>
		<a class="button--primary" id="start-game" href="start/?slug={{slug}}">Start Game</a>
	</body>
</html>